from distutils.core import setup
setup(
  name = 'xl2json',
  packages = ['xl2json'], # this must be the same as the name above
  version = '0.1',
  description = 'Convert Excel data to JSON',
  author = 'Harold Golmdan',
  author_email = 'mikerah@gmail.com',
  url = 'https://github.com/peterldowns/mypackage', # use the URL to the github repo
  download_url = 'https://github.com/peterldowns/mypackage/archive/0.1.tar.gz', # I'll explain this in a second
  keywords = ['excel', 'json', 'convert', 'xls', xlsx], # arbitrary keywords
  classifiers = [],
)